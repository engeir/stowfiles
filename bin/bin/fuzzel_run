#!/bin/bash

PATH="$HOME/bin:$HOME/.local/bin:$HOME/.local/share/mise/shims:$PATH"

{
    tr ':' '\n' <<<"$PATH" | while IFS= read -r dir; do
        [ -e "$dir" ] && find -L "$dir" -maxdepth 1 -type f -executable -printf '%f\n' 2>/dev/null
    done | fuzzel -d | "${SHELL:-"/bin/sh"}" &
}
