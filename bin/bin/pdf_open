#!/usr/bin/env sh
# Search through and open and file with .pdf.

cd ~ || exit 1
c=$(fdfind --one-file-system -H .pdf$ --exclude .config/nnn --exclude OneDrive --exclude BoxSync | grep -o ".*pdf$" | awk -F'/' '{print $NF}' | dmenu -i -F -l 20)

# Exit if none is chosen
[ -z "$c" ] && exit

DIR=$(fdfind --one-file-system -H "$c"$ --exclude .config/nnn --exclude OneDrive --exclude BoxSync | sed 1q)

# Open with default
xdg-open /home/een023/"$DIR"
