#!/usr/bin/env bash

# Toggle screenkey

if ! command -v screenkey >/dev/null 2>&1; then
    exit 0
fi

if pgrep screenkey >/dev/null 2>&1; then
    pkill -f screenkey
else
    screenkey --scr 1 --multiline
fi
