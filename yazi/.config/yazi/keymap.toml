"$schema" = "https://yazi-rs.github.io/schemas/keymap.json"

[manager]
append_keymap = [
  { on = [ # This does not have the desired effect
    "l",], run = "open", desc = "Open the file under the cursor" },
]
